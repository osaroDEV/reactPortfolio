// import { createClient } from '@sanity/client';

// const client = createClient({
//   projectId: '0fc9qpc3',
//   dataset: 'production', // Default dataset is 'production'
//   apiVersion: 'v2021-10-21',
// });

// export default client;

import { createClient } from '@sanity/client';

const client = createClient({
  projectId: 'oherwr3c',
  dataset: 'production', // Default dataset is 'production'
  apiVersion: 'v2021-10-21',
});

export default client;
